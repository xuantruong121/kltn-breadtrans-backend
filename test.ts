import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const quiz = await prisma.quiz.findFirst({ where: { type: 'LISTENING_PRACTICE' }, include: { questions: true } }); console.log(JSON.stringify(quiz, null, 2)); } main().finally(() => prisma.$disconnect());