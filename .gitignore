node_modules
dist

# Keep environment variables out of version control
.env
.env.production
.env.local

/generated/prisma

# Build artifacts & scratch
scratch/
cloudflared.exe
*.exe

# Test environment — local only, never commit secrets
.env.test
.env.test.local
